import pygame

from EUIElement import EUIElement

class EToolTip(EUIElement):
    def __init__(self, position, size):
        super().__init__(position, size)

    def update(self):
        super().update()

    def draw(self):
        super().draw()
