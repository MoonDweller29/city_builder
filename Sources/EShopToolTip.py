import pygame

class EShopToolTip(EToolTip):
    def __init__(self, position, size):
        super().__init__(position, size)

    def update(self):
        super().update()

    def draw(self):
        super().draw()